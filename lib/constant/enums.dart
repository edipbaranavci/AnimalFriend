enum CollectionNames { post, image, user, favoriteUser, favoritePost }

enum FavoritePostCollectionNames { posts }

enum FavoriteFieldNames { postId, userMail, favoriteDate, favoriteBool }

enum FavoriteUserCollectionNames { users }

enum PostFieldNames {
  postAbout,
  postFavoriteCount,
  postDate,
  postImage,
  postLocation,
  postShowCount,
  postTitle,
  userMail
}

enum UserFieldNames {
  displayName,
  email,
  id,
  photoUrl,
  loginDate,
}
